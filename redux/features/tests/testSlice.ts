// redux/features/tests/testsSlice.ts

import { createSlice } from "@reduxjs/toolkit";

export const testsSlice = createSlice({
  name: "tests",
  initialState: {},
  reducers: {},
});

export default testsSlice.reducer;
